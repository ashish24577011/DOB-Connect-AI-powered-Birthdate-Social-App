import requests

# Dummy in-memory user database
users_db = {
    "2000-01-01": ["Alice", "Bob"],
    "1998-05-15": ["John"]
}

def get_matches(dob):
    return users_db.get(dob, [])

def get_celebrities(dob):
    # Mock: Replace with Wikipedia API call
    celeb_map = {
        "2000-01-01": ["Veronica A. (Singer)", "Chris B. (Actor)"],
        "1998-05-15": ["Famous Athlete X", "Scientist Y"]
    }
    return celeb_map.get(dob, ["No celebrity data found"])

def get_news(celebrities):
    # Mock: Replace with NewsAPI calls
    return [{"title": f"Latest update about {celebrities[0]}", "url": "https://news.example.com"}] if celebrities else []

def get_ai_summary(news):
    # Mock: Replace with LLM (e.g., OpenAI GPT) integration
    if not news:
        return "No news available to summarize."
    return "AI Summary: These celebrities are currently in the spotlight for their achievements."
